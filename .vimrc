" Use Vim defaults but make behavior explicit

" Expand tabs to spaces
set expandtab
set tabstop=4        " A tab character looks like 4 spaces
set shiftwidth=4     " Indent by 4 spaces
set softtabstop=4    " Backspace/delete treats 4 spaces as a tab

" Line numbers
set number

" Syntax highlighting
syntax on

" Optional but commonly desired
set autoindent
set smartindent
set ruler
